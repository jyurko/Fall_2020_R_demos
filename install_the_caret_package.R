### we already installed `tidyverse` with the RStudio GUI

### we will now install the `caret` package using the `install.packages()`
### function.

### note when I have backticks `` that corresponds to a variable
### or function name.

### check the already installed packages
# installed.packages()

install.packages("caret")
